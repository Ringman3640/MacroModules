<UserControl x:Class="MacroModules.App.Views.ModulePanelItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
             xmlns:local="clr-namespace:MacroModules.App.Views"
             xmlns:viewmodels="clr-namespace:MacroModules.App.ViewModels"
             xmlns:modules="clr-namespace:MacroModules.App.Views.Modules"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ModulePanelItemVM}"
             d:DesignHeight="450" d:DesignWidth="800"
             Width="100" Height="100" Background="Transparent">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="MouseLeftButtonDown">
            <i:InvokeCommandAction Command="{Binding Path=ModulePanelItem_LeftMouseDownCommand}" PassEventArgsToCommand="True"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    
    <Grid>
        <ContentControl Content="{Binding Module}" IsHitTestVisible="False">
            <ContentControl.ContentTemplate>
                <DataTemplate>
                    <modules:ModuleView/>
                </DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>
    </Grid>
</UserControl>
