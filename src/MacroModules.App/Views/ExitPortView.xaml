<UserControl x:Class="MacroModules.App.Views.ExitPortView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MacroModules.App.Views"
             xmlns:viewmodels="clr-namespace:MacroModules.App.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ExitPortVM}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Canvas Width="0" Height="0">
        <Rectangle x:Name="rectExitPort" Fill="Gray" Height="20" Width="10" Canvas.Top="-10"/>
        <Line Stroke="Black" StrokeThickness="6" StrokeEndLineCap="Round" X1="0" X2="{Binding WireEndPoint.X}" Y1="0" Y2="{Binding WireEndPoint.Y}" Panel.ZIndex="-1"/>
    </Canvas>
</UserControl>
